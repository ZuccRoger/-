let input = ''
var n = '';
while (n = readline()) {
    n.split(' ').forEach((item) => {
        input = item
    })
}
console.log(getMaxHW('aba'))
/**
 * @description: 获得 最多回文串
 * @param {type} 
 * @return {type} 
 */
function getMaxHW(str) {
    if (str.length == 0) return 0
    else {
        if (str.length % 2 == 1) {
            
        } else if (str.length % 2 == 0) {

        }
    }
}